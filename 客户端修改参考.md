# RustDesk 客户端定制参考文档

> 本文整理自定义编译 RustDesk 客户端时的全部源码修改项，供构建/维护时对照使用。
> 涉及三部分：**服务器与账号**、**界面精简（Flutter）**、**默认设置与安全（Rust 配置）**。

## 目录

- [一、服务器与账号](#一服务器与账号)
- [二、界面精简（Flutter）](#二界面精简flutter)
- [三、首页自定义（列表 / 远程桌面 / Logo）](#三首页自定义列表--远程桌面--logo)
- [四、默认设置](#四默认设置)
- [五、安全与密码](#五安全与密码)
- [六、注意事项](#六注意事项)

---

## 一、服务器与账号

### 1.1 置入中继服务器 IP 及 Key

| 项 | 内容 |
| --- | --- |
| 文件 | `libs/hbb_common/src/config.rs` |
| 搜索 | `rs-ny.rustdesk.com` |

- 将其改为**你的服务器 IP**；
- 下方 `RS_PUB_KEY` 改为**服务器的 Key**。

### 1.2 去掉官方的 API 接口

| 项 | 内容 |
| --- | --- |
| 文件 | `src/common.rs` |
| 搜索 | `https://admin.rustdesk.com` |
| 操作 | 改为**空** |

---

## 二、界面精简（Flutter）

### 2.1 删除地址簿、可访问的设备

| 项 | 内容 |
| --- | --- |
| 文件 | `flutter/lib/models/peer_tab_model.dart` |

**删除以下内容：**

```dart
'Address book',
'Accessible devices',
IconFont.addressBook,
IconFont.deviceGroupFill,
!(bind.isDisableAb() || bind.isDisableAccount()),
!(bind.isDisableGroupPanel() || bind.isDisableAccount()),
```

**修改：**

```dart
// 改前
static const int maxTabCount = 5;
// 改后
static const int maxTabCount = 3;
```

### 2.2 去掉底部"如果需要更快连接速度，你可以选择自建服务器"

| 项 | 内容 |
| --- | --- |
| 文件 | `flutter/lib/desktop/pages/connection_page.dart` |
| 搜索 | `translate('setup_server_tip'),` |

**删除：**

```dart
child: Row(
  crossAxisAlignment: CrossAxisAlignment.center,
  children: [
    Text(', ', style: TextStyle(fontSize: em)),
    Flexible(
      child: InkWell(
        onTap: onUsePublicServerGuide,
        child: Row(
          children: [
            Flexible(
              child: Text(
                translate('setup_server_tip'),
                style: TextStyle(
                    decoration: TextDecoration.underline,
                    fontSize: em),
              ),
            ),
          ],
        ),
      ),
    )
  ],
),
```

### 2.3 删掉设置里的"账户"

| 项 | 内容 |
| --- | --- |
| 文件 | `flutter/lib/desktop/pages/desktop_setting_page.dart` |
| 搜索 | `if (!bind.isDisableAccount()) SettingsTabKey.account,` |
| 操作 | 删除该行 |

### 2.4 删掉设置里的"关于"

| 项 | 内容 |
| --- | --- |
| 文件 | `flutter/lib/desktop/pages/desktop_setting_page.dart` |
| 搜索 | `SettingsTabKey.about,` |
| 操作 | 删除该行 |

### 2.5 删除标题栏的设置按钮

| 项 | 内容 |
| --- | --- |
| 文件 | `flutter/lib/desktop/pages/desktop_tab_page.dart` |
| 搜索 | `Widget build(BuildContext context) {` |

**删除：**

```dart
tail: Offstage(
  offstage: bind.isIncomingOnly() || bind.isDisableSettings(),
  child: ActionIcon(
    message: 'Settings',
    icon: IconFont.menu,
    onTap: DesktopTabPage.onAddSetting,
    isClose: false,
  ),
),
```

### 2.6 删除一次性密码中的设置按钮

| 项 | 内容 |
| --- | --- |
| 文件 | `flutter/lib/desktop/pages/desktop_home_page.dart` |
| 搜索 | `message: translate('Change Password'),` |

**删除：**

```dart
if (!bind.isDisableSettings())
  InkWell(
    child: Tooltip(
      message: translate('Change Password'),
      child: Obx(
        () => Icon(
          Icons.edit,
          color: editHover.value
              ? textColor
              : Color(0xFFDDDDDD),
          size: 22,
        ).marginOnly(right: 8, top: 4),
      ),
    ),
    onTap: () => DesktopSettingPage.switch2page(
        SettingsTabKey.safety),
    onHover: (value) => editHover.value = value,
  ),
```

### 2.7 删掉设置里的"网络 - ID/中继服务器"

| 项 | 内容 |
| --- | --- |
| 文件 | `flutter/lib/desktop/pages/desktop_setting_page.dart` |
| 搜索 | `title: 'ID/Relay Server',` |

**删除：**

```dart
if (!hideServer)
  listTile(
    icon: Icons.dns_outlined,
    title: 'ID/Relay Server',
    onTap: () => showServerSettings(gFFI.dialogManager),
  ),
if (!hideServer && (!hideProxy || !hideWebSocket))
  Divider(height: 1, indent: 16, endIndent: 16),
```

### 2.8 删除"你的桌面"ID 旁的设置按钮

| 项 | 内容 |
| --- | --- |
| 文件 | `flutter/lib/desktop/pages/desktop_home_page.dart` |
| 搜索 | `Widget buildPopupMenu(BuildContext context) {` |

**将整个函数：**

```dart
Widget buildPopupMenu(BuildContext context) {
  final textColor = Theme.of(context).textTheme.titleLarge?.color;
  RxBool hover = false.obs;
  return InkWell(
    onTap: DesktopTabPage.onAddSetting,
    child: Tooltip(
      message: translate('Settings'),
      child: Obx(
        () => CircleAvatar(
          radius: 15,
          backgroundColor: hover.value
              ? Theme.of(context).scaffoldBackgroundColor
              : Theme.of(context).colorScheme.background,
          child: Icon(
            Icons.more_vert_outlined,
            size: 20,
            color: hover.value ? textColor : textColor?.withOpacity(0.5),
          ),
        ),
      ),
    ),
    onHover: (value) => hover.value = value,
  );
}
```

**改为：**

```dart
Widget buildPopupMenu(BuildContext context) {
  return Container();
}
```

### 2.9 删除主页上的临时连接密码

| 项 | 内容 |
| --- | --- |
| 文件 | `flutter/lib/desktop/pages/desktop_home_page.dart` |
| 搜索 | `buildPasswordBoard2(BuildContext context, ServerModel model) {` |

**将该函数改为：**

```dart
buildPasswordBoard2(BuildContext context, ServerModel model) {
  return Container();
}
```

---

## 三、首页自定义（列表 / 远程桌面 / Logo）

> **执行顺序要求：先完成 3.1、3.2，再做 3.3 加 Logo。**

### 3.1 删除主页上的整个列表区

即包含【最近访问过、收藏、已发现、地址簿、可访问的设备】的区块。

| 项 | 内容 |
| --- | --- |
| 文件 | `flutter/lib/desktop/pages/connection_page.dart` |
| 搜索 | `final isOutgoingOnly = bind.isOutgoingOnly();` |

**删除：**

```dart
Divider().paddingOnly(right: 12),
Expanded(child: PeerTabPage()),
```

### 3.2 删除主页上"控制远程桌面"

| 项 | 内容 |
| --- | --- |
| 文件 | `flutter/lib/desktop/pages/connection_page.dart` |
| 搜索 | `final isOutgoingOnly = bind.isOutgoingOnly();` |

**删除：**

```dart
children: [
  Row(
    children: [
      Flexible(child: _buildRemoteIDTextField(context)),
    ],
  ).marginOnly(top: 22),
  SizedBox(height: 12),
],
```

### 3.3 加上自定义 Logo

**前提：已完成 3.1、3.2。**

| 项 | 内容 |
| --- | --- |
| 文件 | `flutter/lib/desktop/pages/connection_page.dart` |
| 搜索 | `final isOutgoingOnly = bind.isOutgoingOnly();` |

在 `child: Column(` 与 `.paddingOnly(left: 12.0)),` 之间添加：

```dart
children: [
  Expanded(
    child: Center(
      child: Image.asset(
        'assets/rustdesk.png',
        width: 399,
        height: 106,
        fit: BoxFit.contain,
      ),
    ),
  ),
],
```

**图片文件：** 放到 `flutter/assets/` 目录并改名 `rustdesk.png`。

---

## 四、默认设置

> 以下 Rust 代码默认**函数内已有** `let mut config` 与 `let mut store = false;`。
> 每个块都是"键不存在时才写入"，因此不会覆盖用户已改过的设置。

### 4.1 常规-启用 UDP 打洞（默认打勾）

| 项 | 内容 |
| --- | --- |
| 文件 | `libs/hbb_common/src/config.rs` |
| 搜索 | `Config::load_::<LocalConfig>("_local")` |

**步骤：**

1. 将 `Config::load_::<LocalConfig>("_local")` 改为
   `let mut config = Config::load_::<LocalConfig>("_local");`
2. 在下一行新增 `let mut store = false;`
3. 新增注入块，并在函数末尾补上 `config`。

**改造后完整形态（含 4.2、4.3、4.4 的全部默认项）：**

```rust
fn load() -> LocalConfig {
    let mut config = Config::load_::<LocalConfig>("_local");
    let mut store = false;
    if !config.options.contains_key("enable-udp-punch") {
        config.options.insert("enable-udp-punch".to_string(), "Y".to_string());
        store = true;
    }
    if !config.options.contains_key("theme") {
        config.options.insert("theme".to_string(), "dark".to_string());
        store = true;
    }
    if !config.options.contains_key("enable-check-update") {
        config.options.insert("enable-check-update".to_string(), "N".to_string());
        store = true;
    }
    if !config.options.contains_key("enable-ipv6-punch") {
        config.options.insert("enable-ipv6-punch".to_string(), "Y".to_string());
        store = true;
    }
    if store {
        config.store();
    }
    config
}
```

### 4.2 常规-主题（默认暗黑）

| 项 | 内容 |
| --- | --- |
| 文件 | `libs/hbb_common/src/config.rs` |
| 搜索 | `let mut config = Config::load_::<LocalConfig>("_local");` |

```rust
if !config.options.contains_key("theme") {
    config.options.insert("theme".to_string(), "dark".to_string());
    store = true;
}
```

### 4.3 常规-启动时检查软件更新（默认去勾）

```rust
if !config.options.contains_key("enable-check-update") {
    config.options.insert("enable-check-update".to_string(), "N".to_string());
    store = true;
}
```

### 4.4 常规-启用 IPv6 P2P 连接（默认打勾）

```rust
if !config.options.contains_key("enable-ipv6-punch") {
    config.options.insert("enable-ipv6-punch".to_string(), "Y".to_string());
    store = true;
}
```

### 4.5 Config2 默认项汇总

| 项 | 内容 |
| --- | --- |
| 文件 | `libs/hbb_common/src/config.rs` |
| 搜索 | `let mut config = Config::load_::<Config2>("2");` |

在 `let mut store = false;` 之后新增以下块：

```rust
if !config.options.contains_key("enable-lan-discovery") {
    config.options.insert("enable-lan-discovery".to_string(), "N".to_string());
    store = true;
}
if !config.options.contains_key("allow-remote-config-modification") {
    config.options.insert("allow-remote-config-modification".to_string(), "Y".to_string());
    store = true;
}
if !config.options.contains_key("verification-method") {
    config.options.insert("verification-method".to_string(), "use-permanent-password".to_string());
    store = true;
}
if !config.options.contains_key("approve-mode") {
    config.options.insert("approve-mode".to_string(), "password".to_string());
    store = true;
}
```

对应设置项：

| 设置项 | 选项键 | 默认值 | 效果 |
| --- | --- | --- | --- |
| 安全-拒绝局域网发现 | `enable-lan-discovery` | `N` | 打勾（界面取反显示） |
| 安全-允许远程修改配置 | `allow-remote-config-modification` | `Y` | 打勾 |
| 安全-密码：使用固定密码 | `verification-method` | `use-permanent-password` | 切换为"使用固定密码" |
| 安全-密码：只允许密码访问 | `approve-mode` | `password` | 切换为"只允许密码访问" |

---

## 五、安全与密码

> **重要**：`unlock_pin`、`trusted_devices`、`password` 等字段存放的是**加密后**的字符串，
> 不能填明文。加密值的获取方法见 [5.4](#54-如何获取加密后的-pin-与密码)。

### 5.1 安全-使用 PIN 码解锁设置（默认打勾）

| 项 | 内容 |
| --- | --- |
| 文件 | `libs/hbb_common/src/config.rs` |
| 搜索 | `config.unlock_pin = unlock_pin;` |

**在其后新增：**

```rust
if config.unlock_pin.is_empty() {
    config.unlock_pin = "！！！加密后的PIN码！！！".to_string();
    store = true;
}
```

### 5.2 安全-密码：默认固定密码

| 项 | 内容 |
| --- | --- |
| 文件 | `libs/hbb_common/src/config.rs` |
| 搜索 | `config.unlock_pin = unlock_pin;` |

**在其后新增：**

```rust
if !config.options.contains_key("trusted_devices") {
    config.options.insert("trusted_devices".to_string(), "！！！加密后的密码！！！".to_string());
    config.store();
}
```

### 5.3 安全-密码字段默认值

| 项 | 内容 |
| --- | --- |
| 文件 | `libs/hbb_common/src/config.rs` |
| 搜索 | `log::error!("Failed to generate new id");` |

**在其后的 3 个 `}` 之后新增：**

```rust
if config.password.is_empty() {
    config.password = "！！！加密后的密码！！！".to_string();
    store = true;
}
```

### 5.4 如何获取加密后的 PIN 与密码

1. 先在 RustDesk 软件里**设置一个 PIN 码 / 密码**；
2. 打开配置文件：

   | 目标 | 配置文件路径 |
   | --- | --- |
   | PIN 码 | `C:\Users\当前Windows账户\AppData\Roaming\RustDesk\config\RustDesk2.toml` |
   | 密码 | `C:\Users\当前Windows账户\AppData\Roaming\RustDesk\config\RustDesk.toml` |

3. 复制其中的加密值：

   ```toml
   # RustDesk2.toml
   unlock_pin = '1V8AS4/+EWF84A85WD4AW=='

   # RustDesk.toml
   password = '1V8AS4/+EWF84A85WD4AW=='
   ```

4. 把加密后的字符串（示例中的 `1V8AS4/+EWF84A85WD4AW==`，**仅为占位示例**）填入对应代码块。

---

## 六、注意事项

1. **加密值不可明文**：`unlock_pin`、`trusted_devices`、`password` 必须填入软件生成的加密字符串，否则功能失效。
2. **Flutter 删改需保持语法完整**：删除代码块时注意成对的花括号与逗号，删除后应能通过 `flutter analyze` / 编译。
3. **顺序要求**：必须**先删除列表区与"控制远程桌面"（3.1、3.2），再加自定义 Logo（3.3）**。
4. **`store` 变量前提**：4.x、5.x 的注入块都依赖函数内已声明 `let mut store = false;`，若尚未声明需自行补齐。
5. **上游版本差异**：以上搜索/删除片段基于当前上游 `rustdesk/rustdesk`（master）源码，上游更新后标记可能失效，需重新核对。
